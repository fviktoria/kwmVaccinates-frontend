import { VaccinationAdminErrorMessages } from './vaccination-admin-error-messages';

describe('VaccinationAdminErrorMessages', () => {
  it('should create an instance', () => {
    expect(new VaccinationAdminErrorMessages()).toBeTruthy();
  });
});
